name: canteen_booking_app
description: A new Flutter project for Canteen Booking System.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  intl: ^0.18.0
  shared_preferences: ^2.2.0
  google_sign_in: ^6.1.5
  qr_flutter: ^4.1.0
  uuid: ^4.2.1
  cloud_firestore: ^4.9.0
  firebase_core: ^2.15.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  sqflite: ^2.3.0
  path: ^1.8.3
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
